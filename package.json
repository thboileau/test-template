{
  "name": "portal-template",
  "version": "1.0.0",
  "description": "API Portal raw template",
  "keywords": [
    "portal",
    "template"
  ],
  "license": "UNLICENSED",
  "author": "Talend",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/talend/apimgmt-portal-headless-frontend.git"
  },
  "scripts": {
    "build": "node_modules/.bin/parcel build index.html",
    "dev": "node_modules/.bin/parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
